name: Security Scans
on:
  workflow_dispatch:
   inputs:
    environment:
      description: Environment to deploy to
      type: environment
      required: true
jobs:
  Test:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
   # needs: Build
    environment: test
    steps:
      - run: echo "🧪 Testing..."
